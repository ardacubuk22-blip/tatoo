# راه‌اندازی پنل ادمین (Supabase)

سایت استاتیکه و روی GitHub Pages اجرا می‌شه؛ دیتابیس و لاگین از Supabase میاد
(پلن رایگان کافیه). این مراحل یک‌بار انجام می‌شن.

## ۱. ساخت پروژه Supabase

1. وارد [supabase.com](https://supabase.com) بشید و یه پروژه‌ی جدید بسازید
   (Region نزدیک به ترکیه مثل `eu-central` بهتره).
2. رمز دیتابیس رو جایی امن ذخیره کنید.

## ۲. ساخت جدول‌ها

در پنل Supabase: **SQL Editor → New query** → کل محتوای فایل
`supabase/schema.sql` رو کپی و اجرا (Run) کنید.

این کار جدول‌های `products`، `site_settings` و `orders` رو می‌سازه، قوانین
دسترسی (RLS) رو فعال می‌کنه، باکت `product-images` رو برای عکس‌ها ایجاد می‌کنه
و محتوای فعلی سایت رو به‌عنوان داده‌ی اولیه وارد می‌کنه.

## ۳. ساخت کاربر ادمین

**Authentication → Users → Add user → Create new user**

- ایمیل و رمز عبور مشتری رو وارد کنید
- گزینه‌ی **Auto Confirm User** رو حتماً تیک بزنید

سپس در **Authentication → Sign In / Providers → Email**، گزینه‌ی
**Allow new users to sign up** رو خاموش کنید تا کسی نتونه خودش ثبت‌نام کنه.

## ۴. وصل کردن سایت به Supabase

در پنل Supabase: **Project Settings → API** و این دو مقدار رو بردارید:

- `Project URL`
- کلید `anon` / `public`

بعد در فایل `js/config.js` جایگذاری کنید:

```js
export const SUPABASE_URL = "https://xxxxx.supabase.co";
export const SUPABASE_ANON_KEY = "eyJhbGciOi...";
```

سپس کامیت و پوش کنید.

> کلید `anon` عمومیه و قرار دادنش در کد مشکلی نداره — دسترسی واقعی با قوانین
> RLS کنترل می‌شه: همه فقط می‌تونن محصولات رو بخونن و سفارش ثبت کنن؛ ویرایش و
> دیدن سفارش‌ها فقط برای کاربر لاگین‌کرده ممکنه.
>
> ⚠️ **کلید `service_role` را هرگز در کد نگذارید و برای کسی نفرستید.** اون کلید
> تمام قوانین RLS رو دور می‌زنه و دسترسی کامل به دیتابیس می‌ده. فقط کلید
> `anon` / `public` لازمه.

## ۵. استفاده

- پنل ادمین: `https://<آدرس-سایت>/admin/`
- ورود با همون ایمیل و رمزی که در مرحله ۳ ساختید

از پنل می‌شه:

- **Ürünler**: افزودن/ویرایش/حذف محصول، آپلود عکس، قیمت، ترتیب نمایش و
  علامت «Satıldı»
- **Siparişler**: دیدن سفارش‌ها و درخواست‌های اطلاعات، تغییر وضعیت و حذف
- **Site Ayarları**: ویرایش متن‌های سایت، آدرس، تلفن، ایمیل و اینستاگرام

تغییرات بلافاصله روی سایت اعمال می‌شن (نیازی به کامیت جدید نیست).

## نکات

- تا وقتی `js/config.js` پر نشده، سایت همون محتوای استاتیک فعلی رو نشون می‌ده
  و پنل ادمین پیام «کرولوم تمام نشده» می‌ده. یعنی سایت هیچ‌وقت خراب نمی‌شه.
- عکس‌های اولیه از پوشه‌ی `assets/images` خونده می‌شن؛ عکس‌هایی که مشتری از
  پنل آپلود می‌کنه در Storage خود Supabase ذخیره می‌شن.
- پروژه‌های رایگان Supabase بعد از حدود ۷ روز بی‌استفاده ماندن موقتاً pause
  می‌شن و با یک کلیک در داشبورد دوباره فعال می‌شن. اگه سایت ترافیک منظم داشته
  باشه این اتفاق نمی‌افته.
